@using JobApplications.DTOs
@using JobApplications.Data.Models
@using JobApplications.Extensions
@model List<JobFormDto>

@{
    // job ->
    var userid = User.GetId();

}
<h1>Our Active and InActive Jobs - @Model[0].Company.CompanyName</h1>
@* @if (Model.Count > 0)
{
    <div>
        @foreach (var item in Model)
        {
            <p class="bg-info">@item.Company.CompanyName - @item.Title - for salary: @item.Salary</p>
            @if (userid == item.Company.IdentityUserId)
            {
                <a asp-controller="Job" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-info">Delete</a>
                <a asp-controller="Job" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-info">Edit</a>
            }
            
        }
    </div>
} *@

<div class="row">
<!-- 
		var userId = User.GetId();
		var userIsDealer = Dealers.IsDealer(userId); -->

		@foreach (var job in Model)
		{
			<div class="col-md-4">
				<div class="card mb-3">
					<img src="@job.Banner" alt="@job.Company.CompanyName - @job.Title">
					<div class="card-body text-center">
						<h5 class="card-title text-center">@job.Company.CompanyName - @job.Title - for salary: @job.Salary</h5>
						<a asp-controller="Cars" asp-action="Details" asp-route-id="@job.Id class="btn btn-primary">View</a>
						
					</div>
				</div>
			</div>
		}
</div>